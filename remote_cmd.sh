#!/bin/bash
# 远程命令执行脚本
# 使用方法: ./remote_cmd.sh "命令"

if [ -z "$1" ]; then
    echo "❌ 请提供要执行的命令"
    echo "使用方法: $0 '命令'"
    exit 1
fi

echo "📤 执行远程命令: $1"
ssh ubuntu-server-trae "$1"
